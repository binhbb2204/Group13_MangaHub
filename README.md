# Manga-Hub-Group13
First run this in another terminal:
go run cmd/api-server/main.go 

Then run this one in another terminal:
$env:PATH = "D:\Net-Centric-Lab\Group13_MangaHub\bin;$env:PATH"

From that you can run this to test:
Example: mangahub auth register --username <username> --email <email>
It will show:
Password: --Type password
Confirmed password: --Type password